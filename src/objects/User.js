class User {
    
    constructor({ id, username }, currentStatus) {

        this.id             = id
        this.username       = username
        this.currentStatus  = currentStatus
    }

}

export default User